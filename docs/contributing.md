# Contributing Guide

1. Create a feature branch from `main`.
2. Keep changes inside the appropriate domain module.
3. Run `composer ci` before opening a pull request.
4. Add or update tests for every behavior change.
5. Avoid tenant-specific logic and prefer configuration over customization.
